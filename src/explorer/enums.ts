'use strict';

export enum ResourceType {
    System = "jenkins:system",
    Views = "jenkins:views",
    View = "jenkins:view",
    Job = "jenkins:job",
    Build = "jenkins:build",
    Nodes = "jenkins:nodes",
    Node = "jenkins:node",
}
